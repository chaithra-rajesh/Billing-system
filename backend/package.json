{
  "name": "billing-app-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Supabase backend for the billing app: schema, RLS, edge functions.",
  "scripts": {
    "start": "supabase start",
    "stop": "supabase stop",
    "status": "supabase status",
    "reset": "supabase db reset",
    "migration:new": "supabase migration new",
    "migration:up": "supabase migration up",
    "functions:serve": "supabase functions serve --env-file .env",
    "functions:deploy": "supabase functions deploy",
    "lint": "supabase db lint",
    "types": "supabase gen types typescript --local"
  },
  "devDependencies": {
    "supabase": "^1.200.0"
  }
}
