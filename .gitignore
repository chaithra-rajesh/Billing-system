# --- Secrets — never commit ---
.env
.env.*
!.env.example
# Frontend NEXT_PUBLIC_* values only — they are inlined into the client
# bundle by Next.js and visible to any browser, so they are not secrets.
# Required because Cloudflare Pages' next-on-pages build does not pass
# dashboard env vars through to `next build`.
!frontend/.env.production
*.pem
*.key
secrets/

# --- Node / Next.js ---
node_modules/
.next/
out/
dist/
build/
coverage/
*.tsbuildinfo
next-env.d.ts
.vercel/

# --- Supabase CLI working files ---
supabase/.branches/
supabase/.temp/
supabase/.env

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Editors ---
.idea/
.vscode/
*.swp
*.swo

# --- Claude Code local settings ---
.claude/

# --- Misc ---
*.bak
*.tmp
